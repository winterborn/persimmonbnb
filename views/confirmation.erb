<!-- GET /confirmation -->

<html>
  <head></head>
  <body>
    <h1>Thanks for adding a listing!</h1>

    <% @spaces.each do |space| %>
        <div>
            <p><%= space.space_name %>: <%= space.space_description %> - <%= space.space_price %></p>
        </div>
      <% end %>
  </body>
</html>